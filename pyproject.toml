[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emotional_budget_tracker"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["emotional_budget_tracker*"]
